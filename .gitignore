.github/
__pycache__/
*.pyc
.pytest_cache/
# 原始大体积数据只留本地做增量更新底座，不入库（46MB 级）；对外分发走 release/ 里的压缩 gz。
# 小体积引擎数据（divisions/townships/change_events/historical_changes 等 <3MB）随仓库走。
data/villages.csv
# release/ 常驻最新一版附件（两个 gz + 清单/校验和，共约 12MB），随数据更新重建并入库；
# 历史版本归档在 GitHub Release（按 tag 区分，由维护者手动上传）。
