# 社区版

**一句话定位**：口径准确的中国行政区划数据工具。

**本仓库是社区版，永久免费开源、可商用。** 其上另有专业版提供批量清洗、变更流、双码对照、
可溯源与可审计等能力，此处不展开；有需要请看项目主页或联系作者。

## 社区版包含

**能力**（`engine.py`，零依赖）

| 能力 | 说明 |
|---|---|
| 地址解析 / 清洗 / 编码 | 单条，省市区县乡镇五级识别，输出置信度分级 |
| 区划码直查 | 6 / 12 位，含历史码回溯 |
| 时间机器 | 任意年份回放（1981 年至今） |
| 变更事件库检索 | 5,276 条，按年份 / 关键词 / 区划码 |

> 社区版面向**很基础的使用**：单条查询、本地验证、自托管跑通。
> 批量清洗、溯源与审计属于专业版能力。

**数据**

| 数据 | 规模 | 位置 |
|---|---|---|
| 三级区划（省 / 地 / 县） | 3,214 条 | 仓库内 `data/` |
| 乡镇街道 | 38,749 条 | 仓库内 `data/` |
| 历史变更事件 | 5,276 条（1981 年至今） | 仓库内 `data/` |
| 别名词典 | 1,254 条（人工策展 45 + 事件库派生 1,209） | 仓库内 `aliases.json` |
| **村级全量** | **604,626 条** | [GitHub Release](../../releases) 按版本数据包 |

村级数据不在仓库内是因为体积（46 MB 级），不是因为授权——**村级同样免费**。

## 社区版不包含

**批量清洗**、**可溯源与可审计**、变更流增量、双码对照、县档案、托管服务与支持承诺。

> 专业版另提供批量清洗、变更流、可审计交付等能力，此处不展开。

## 自托管

数据不出内网。

```bash
git clone https://github.com/nbsetsail/real-china-division.git
cd real-china-division
# 引擎零依赖；拼音纠错为可选增强
pip install pypinyin
```

```python
from engine import resolve, resolve_by_code

resolve("襄樊市樊城区")      # 历史名 → 湖北省-襄阳市-樊城区
resolve_by_code("510124")    # 历史码回溯 → 四川省-成都市-郫都区，2016 年撤县设区
```

> 以上示例为真实输出，可直接复现。

## License

- 代码：**MIT**
- 数据：**CC BY 4.0**（署名即可商用）

> **Don't trust, verify.** 每个 Release 附带校验和，季度准确率报告公开可查。
