# 社区版 vs 专业版

**一句话定位**：口径准确的中国行政区划数据工具。

本项目采用 open-core：社区版（本仓库）永久免费开源，专业版在其之上提供**服务型能力**。
两者的差别**不在代码分支，而在部署了哪些数据包**——是同一份代码跑出来的两种形态。

## 能力矩阵

| 能力 | 社区版 | 专业版 | 说明 |
|---|:--:|:--:|---|
| **核心解析** | | | |
| 地址解析 / 清洗 / 编码 | ✅ | ✅ | 省市区县乡镇五级识别 + 置信度分级 |
| 批量解析 | ✅ | ✅ | 社区版自托管无数量限制 |
| 区划码直查（6/12 位） | ✅ | ✅ | 含历史码回溯 |
| 变更事件库检索 | ✅ | ✅ | 5,274 条（1981 年至今），按年份/关键词/区划码 |
| 时间机器（任意年份回放） | ✅ | ✅ | 基于已开源的历史事件库 |
| **专业版能力** | | | |
| **变更流增量** `/api/changes` | ❌ | ✅ | 按 `since` 增量拉取，带游标；**旗舰能力** |
| 变更区间摘要 | ❌ | ✅ | 月度推送「你的辖区变了 N 处」的数据来源 |
| 双码对照 `/api/dual` | ❌ | ✅ | 民政口径 × 统计口径 |
| 县档案 `/api/profile` | ❌ | ✅ | 首见年份/事件/别名/下级规模/双码覆盖率 |
| 可审计交付物 | ❌ | ✅ | 溯源链 + 版本快照哈希 + 季度准确率报告 |
| 解释与置信度溯源 | 部分 | ✅ | 专业版给出「为什么这么匹配」+ 官方出处 |
| **服务与责任** | | | |
| 持续更新 | 不定期快照 | 月度跑、季度发 | 数据源收缩后，持续维护本身即稀缺品 |
| 稳定性承诺 | 无 | 版本锁定 + 不 breaking change + 响应时限 | 生产环境刚需 |
| 出错有人负责 | 用坏自理 | 免费修正 + 责任划分 + 响应时限 | |
| 吞吐配额 | — | 放开 | 付费墙 |
| 托管服务 / Docker / SDK | ❌ | ✅ | 免运维 |

> **社区版当前以 Python 库（`engine.py`）形式提供**，REST API 与 CLI 在规划中。
> 专业版已提供完整 REST API。两者共用同一 API 契约。

## 数据清单

| 数据 | 社区版 | 专业版 | 备注 |
|---|:--:|:--:|---|
| 三级区划（省/地/县） | ✅ | ✅ | 3,213 条，民政口径 |
| 乡镇街道 | ✅ | ✅ | 38,749 条 |
| 历史变更事件 | ✅ | ✅ | 5,274 条，183 条带可核验官方出处 |
| 别名词典 | ✅ | ✅ | 888 条，含来源 |
| **村级全量 604,626 条** | ✅ | ✅ | **免费**，见 GitHub Release 数据包 |
| 双码交叉表 | ❌ | ✅ | 民政 ↔ 统计口径 |
| 双码覆盖率 | ❌ | ✅ | |
| 统计局口径村级 | ❌ | ✅ | 2023 基线 |
| 县档案知识库 | ❌ | ✅ | |
| 评测集 / 准确率报告 | ❌ | ✅ | |

**为什么村级免费？** 这数据我们能跑出来，别人一样也能跑出来。凡是符合这句的，都不该是付费点。

## 为什么这样切

**1. 数据必然泄露，服务不会。**
静态数据（哪怕采集成本很高）一旦交付即被复制。所以所有真付费点都是**服务属性**：
持续更新、增量推送、稳定性承诺、有人负责。这跟「轻」是自洽的——服务能自动化，不靠人堆。

**2. 变更流是旗舰，因为它是唯一能主动触达客户的能力。**
其他能力都是「用了才知道好」，只有变更流能每月主动说「你的辖区这个月变了 3 处」。
对不做销售动作的团队，这是留存与续费的主要杠杆——让产品自己续费。
数据点：2020 年以来六年仅 50 条县级变更，其中改码 25 条。**价值不在数量，在于客户不用自己盯着。**

**3. 双编码是进入门槛，不是长期壁垒。**
交叉表是静态的，一旦泄露或被自行构建，客户就不需要你了。真正持续的是「每年新增码的对照维护」。

**4. 单一代码库 + capability 开关。**
绝不维护两套代码。启动时检测数据包是否存在，`/api/capabilities` 返回能力清单，
缺包的能力调用时返回 `501` 并指引替代端点。这样专业版的升级路径对用户而言**只是换一个 `base_url`**。

## 升级路径

自托管社区版 → 业务量上来 / 需要持续更新 → **改 `base_url` 切到专业版**，代码一行不用动。

```python
# 社区版自托管
client = DivisionClient(base_url="http://localhost:8911")
# 专业版
client = DivisionClient(base_url="https://api.example.com", api_key="...")
```

## 自托管

社区版可完全自托管，**数据不出内网**——这是必须上云的托管服务做不到的，
对有数据合规顾虑的团队是硬牌。

```bash
pip install -r requirements.txt
python -c "import engine; print(engine.resolve('浙江杭州市余杭区文一西路969号'))"
```

## License

- 代码：**MIT**
- 数据：**CC BY 4.0**（署名即可商用）

**关于白标转售**：MIT 与 CC BY 都允许他人转售本项目的副本。这是已知且接受的事实——
本项目的护城河从不在于「别人拿不到」，而在于**持续更新、可审计、有人负责**这三件事，
它们无法被复制一份静态副本获得。也正因如此，本项目不使用 Commons Clause / BSL 等附加限制。

> **Don't trust, verify.** 每个 Release 都附带 `SHA256SUMS.txt` 与整包校验和，
> 季度准确率报告公开可查。我们主张的是整理、加工与持续维护层，行政区划代码本身是官方发布的事实数据。
