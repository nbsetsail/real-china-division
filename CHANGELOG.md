# Changelog

本仓库遵循 [语义化版本]：数据修订以次版本号递增，口径变更以主版本号递增并发布公告。

## [0.4.0] - 2026-09-19

### 新增：MCP Server

- 新增 `mcp_server.py` + `MCP.md` + `pyproject.toml`（PyPI 包名 `quhua-mcp`，`uvx quhua-mcp` 一行接入）：
  5 个工具（resolve / code / events / suggest / capabilities）+ 1 个资源（数据字典）。
  查不到返 `unresolvable` 不编造；同名歧义返回候选集——把「诚实拒绝」带进 MCP 生态。
  面向 Claude Desktop / Claude Code / Cursor 等 MCP 客户端，无需自建服务。
- 新增 `tests/test_no_pinyin_fallback.py`：可选依赖降级专项测试——pypinyin 缺失时
  模糊匹配（消歧依据只有读音）**整体放弃**而非短路，杜绝「东北省 → 河北省」类假阳性。

- MCP 相关文件收敛到 **`mcp/` 子目录**（`mcp_server.py` / `MCP.md` / `pyproject.toml`）：
  引擎经父目录引导兼容「克隆直跑 `python mcp/mcp_server.py`」；wheel 经 force-include
  打包引擎与数据（**wheel-only 发布**——sdist→wheel 的二次构建会丢失 `../` 相对路径）。
  已在隔离 venv 用装好的 wheel 验证 stdio 握手、tools/list 与 tools/call 全链路。

### 引擎

- 无拼音环境守卫补严：模糊匹配的闸②（拼音键比对）在无 pypinyin 时失效，
  字距 1 分不开「东北省/河北省」→ 无拼音时跳过该路径，宁漏纠不误报
- 「方位/大区 + 省」统一拒绝：`东北省` 这类泛称+省后缀无论走模糊匹配还是拼音纠错都是假阳性，
  在入口统一诚实拒绝（仅对「省」后缀生效，不影响「北方镇」等真实地名）
- 输入以「省」结尾 = 断言省级建制，省级全部未命中时县级短形式子串命中按张冠李戴拒绝
  （「太平洋省」不再返回太平区 conf 1.0）
- 事件库年份口径修正：事件条目最早 1981 年（1980 年快照是差分基线），note 与文档统一为 1981-2026

### 文档

- 别名词典口径勘误：1,254 → **1,214**（60 条人工种子 + 事件库运行时派生）
- README「110/110」过期引用 → 259/259；季度准确率报告重生成（回归集扩至 259 例）

## [0.3.0] - 2026-09-17

### 引擎

- 移除 `alias_evidence()` 溯源接口及其来源收集逻辑
- **修复 35 处真实解析缺陷 / 新增 4 项能力**，每处配 ≥3 条防回归用例（回归集 110 → 259 全绿）：
  - 泛称与同音：区域泛称（`华南` / `西南` / `中原` 等）整串拒绝，拒绝后不再走拼音纠错；拼音键改按音节分隔，`西南`（xi-nan）与 `新安`（xin-an）不再碰撞
  - 别名替换：单字别名（沪 / 京 / 渝 / 津）仅整串生效；别名键是更长已知名前缀时不替换；别名与现行同名区划冲突时按上下文判定（`安徽省合肥市巢湖市中垾镇` 按现行县级市，`巢湖市庐江县` 按已撤销地级市）
  - 拼音纠错：跳过本身就是现行区划名、或属别名词典合法写法的片段；候选按唯一解析优先于歧义；只解析到省级的浅结果也重试纠错，且仅层级更深才采纳
  - 层级与短形式：去后缀短形式（市 / 区 / 县 / 旗 / 盟）不得遮蔽更长的已知区划名；地级市匹配按出现位置择优，匹配窗口前一字符可扩展成更长名时不作数（`峨眉山市` 不再被 `眉山市` 吃掉）
  - 省级路径反查所属地级：`浙江省西湖区` 不再返回 county 码为 null 的结果；省未命中时先做地级全名直配，再走县级直配
  - 时间机器区分全名与短形式命中，同名多命中时全名优先（1992 年查 `芜湖县` 不再返回芜湖市）
  - 多实体合称支持通用分隔符拆分；连接词（和 / 与 / 及）落在已知地名内部时不切分
  - 建制边界判定改用 CJK 区间（`（山东省）莒县` 可正常解析）；边界前缀判断只用省 / 市 / 县三级名，避免 2-3 字乡镇名跨词巧合触发误替换
  - 事件库 `new_path` 只给末段时按现行库补全省 / 市，解锁 47 条旧名路径
  - 未随包分发逐年快照时，时间机器返回"本发行版不可用"，不再抛异常
  - 形近字纠错：新增第二道纠错（拼音不同音、部首/字形相近的高频地址错字，首批「玔→圳」），「深玔市南山区科苑南路」得以纠回深圳南山；采纳条件严于同音纠错
  - 纠错门控修正：纯道路名守卫只在完全无命中时阻断（ambiguous 态地址末字的「路/街」不再封死纠错）；「特殊口径」结论（苏州工业园区等）不进纠错
  - 开发区/园区落所属地级：「南昌高新技术产业开发区」等落到江西省-南昌市，不再丢失地级信息（不虚构县级码的口径不变）
  - 街道证据消歧：同名县多处命中时，文本中的乡镇/街道名（≥3 字）唯一归属某候选即消歧（「深南山区粤海街道」→深圳市南山区-粤海街道）；证据冲突或无证据维持歧义
  - 沿革链追尾 + 转隶补全（B-28）：历史别名沿事件链追到现行终态（昌潍地区→潍坊市这类两跳链此前整段丢失）；145 条被年度差分误标为「撤销」的转隶事件经 GB 快照按名追踪自动补全去向，旧区划名迁移能力从 2 条扩至覆盖 30 省 149 个旧名；B-29 批量公报检索再补 240 条（国务院公报原文在线可搜，批复批量式一搜一组），迁移映射累计覆盖 416 旧名/31 省，并新增 ambiguous 文本证据消歧（「莱芜市钢城区」→钢城区）
  - 方位泛称补全（B-32）：北方/南方/东部/西部/中部/沿海/边疆/内陆/内地等入泛称表，整串精确匹配即诚实拒绝（此前会被形近候选集捡成县名，如「北方」→北屯市）
  - 撤销迁移定向细化（B-33）：输入含市名前缀即定向现行建制（「宁波市江北区」→浙江宁波）；无前缀裸名仍出双候选
  - 副表解析通道（B-31，专业版/L1 能力）：港澳台下辖不在主底座（无国标码），副表数据存在时接入解析——台湾省→县市→乡镇（台湾「区」为乡级）、香港→18 区、澳门→堂区；村/街道名可推断所属区/堂区（地址要素不占行政层级）；同名多路径输出候选集，大陆同名（松山区/大安区）优先归大陆。本发行版不带副表数据，该通道自动关闭，港澳台输入仍只到省级
  - 裸名冲突双候选（B-30）：区县名既有现行建制、又是已撤销迁移旧名时（「江北区」=宁波现行 与 重庆撤销→两江新区并存），裸名输出双候选集而非静默选边；输入带省/市前缀则定向解析，同省同名转隶（岳阳县→岳阳县）不构成冲突
  - 形近候选集（B-27）：『写对就一种，写错无数种』——主解析失败时按算法相关度表（IDS 部件重叠 + 笔画差，7300 对）输出形近候选（ambiguous，conf 0.5），不猜唯一答案；人工确认的白名单对（如 玔→圳）仍自动纠错
  - 无读音校验时的模糊匹配假阳性（B-34）：`pypinyin` 未安装时，模糊匹配（编辑距离 1）的读音校验被
    整条短路而非收紧，「东北省」被当作「河北省」的错写采纳——二者仅差一字，**只有读音能区分**。
    现改为无读音校验即放弃模糊匹配：宁漏纠，不可把不存在的区划说成存在。错字纠错能力随之在
    未安装 `pypinyin` 时整体关闭（与既有文档表述对齐）。新增降级路径专项测试，
    因为该缺陷在装有 `pypinyin` 的环境**不可见**，普通回归用例无法守护
  - 泛称拒绝漏掉带后缀形态（B-35）：「华南」「东北」等区域泛称此前只做整串匹配，带「省」后缀
    即漏网，随后被纠错通道捡成真实区划（「华东省」→东胜区、「华南省」→桦南县）。现识别
    「方位/大区 + 省」形态并拒绝；仅对「省」后缀生效，方位词开头的真实地名（「西南镇」→广东陆丰）
    不受影响

### 工具

- **新增 MCP Server（`mcp_server.py`）**：把解析能力直接提供给 Claude Desktop / Claude Code /
  Cursor 等支持 MCP 的 AI 客户端，含 5 个工具（地址解析、编码直查与历史码回溯、下级区划枚举、
  变更事件检索、**区划存在性校验**）与 1 个数据口径资源。立场是「模型负责语言、权威库负责确认」——
  查不到时返回 `unresolvable` 而非编造，同名返回候选集而非替用户选。用法见 [MCP.md](MCP.md)
- **B-36**：「X省」断言省级建制但无此省时，县级短形式子串命中不得顶替（「太平洋省」→太平区 conf 1.0 假阳性）——诚实拒绝，拼音/形近/候选集三道纠错通道同步封口（4 例回归）
- **MCP 服务同时兼容 SDK 1.x 与 2.x**：2.x 把 `FastMCP` 改名为 `MCPServer`、导入路径也随之变化，
  且官方不提供兼容垫片——只写 1.x 的导入会在装到 2.x 的环境里直接 `ImportError`。现已双版本支持，
  两条路径均实测通过（1.9.4 / 2.2.0）
- **打包分发**：新增 `pyproject.toml`（wheel 约 4.1 MB，只含引擎读取的 5 个数据文件，不含 44 MB 的村级
  CSV），并提供 PEP 723 内联依赖声明——`uv run mcp_server.py` 可在**不建虚拟环境、不 pip install** 的
  前提下直接运行；发布到 PyPI 后即可 `uvx quhua-mcp` 一行接入
  - 撤销迁移别名（B-25）：行政区撤销/并入类事件（人工策展成对记录）自动生成「旧区名→新归属」历史别名——「渝北区」→两江新区、「500112000000」旧码经历史码映射到两江新区；旧地址自动迁移到新区划（残缺撤销事件仍被守卫排除）
  - 撤销迁移解析（B-26）：与现行区同名的已撤销县（重庆江北区 vs 宁波江北区）按省索引做「旧县名→新归属」迁移解析，「重庆江北区观音桥步行街」→ 重庆市-两江新区（conf 0.8）；跨省不受影响
  - 置信度诚实性：省 / 市级命中后，残留文本中仍以下级建制通名收尾的声称不再被静默吞掉——
    `香港中西区` / `台湾台北市` / `广东省龙泉驿区`（该区属成都市）由 conf=1.0 降为 0.6（省级）或
    0.7（市级）并明示"未能核实"；港澳台下辖注明无国标码、以独立副表只读提供。残留能被下级正常
    消费（`浙江省滨江区`）或纠错可修复（`云南省五定县`→武定县）时行为不变

### 数据

- **事件库新增 `码位改派` 类型（623 条，原标注为 `更名`）**：GB/T 2260 逐年快照差分把「同一区划码在不同年份指向不同实体」（码位被重新分配）误判为更名，产出 `A→B` 与 `B→A` 互指的假记录（26 组互换对），并据此生成了错误历史别名（如 `芳村区 → 广州市天河区`）。判据：真更名意味着新名首次出现，故「新名在该事件前一年快照中已存在」即为伪更名。处理方式为改判类型而非删除记录（事实为真、类型错），事件总量 **5,274 条不变**，`change_events.csv` 的 `type` 列同步
- 历史别名映射 915 → **843 条**（挤出 72 条由伪更名产生的错误别名；`阿城县` 由「大庆市-杜尔伯特蒙古族自治县」修正为「哈尔滨市-阿城区」）
- 别名词典 v0.5：补历史旧称（奉天 / 北平 / 迪化 / 归绥 / 安东）、省级简称（渝 / 津）、城市别称（穗）
- **省级从 33 增至 34**：补入台湾省（码 710000000000，仅省级）。台湾省未被国家地名信息库收录，此前 `台湾省` 返回 `unresolvable`；现按 GB/T 2260 国家标准补录，来源记于 `divisions_v1.json` 的 `meta.patched`。下辖县市 GB/T 2260 未给出可核验的官方码，按「不编造」纪律一律不列（`台湾省台北市` 只返回省级）
- 数据完整性校验（CI）同步：省级断言 33 → 34，新增台湾省码位断言

### 文档

- 修正事件流「带文号」的不实描述（5,274 条中仅 123 条有批准文号）；数据字典与 Roadmap 表述中性化
- README：事件类型分布、别名映射数量与数据字典同步到本次修订，新增 `码位改派` 口径说明
- **公开发布文档的统计数字与实际数据逐项核对后同步**：事件库 5,276 条、地级 333、别名映射 1,254 条（人工策展 45 + 事件库派生）、`doc_no` 130 条、`source_url` 359 条；同时重算 `historical_changes_v1.json` 的 `meta.counts` 与 `meta.by_type`（此前停留在文号回填前的旧值，且 `by_type` 仍把 `码位改派` 并在 `更名` 里）

## [0.2.1] - 2026-09-14

### 数据

- **变更事件库 5,195 → 5,274 条**：新增民政部区划地名网《县级以上行政区划变更情况》官方年度页（1999-2026，GBK 编码）作为一手语料源，抓取 485 条官方变更记录（含省级政府公告来源+日期），比对后入库 64 条（更名 8 / 驻地迁移 9 / 撤销 3 / 新设 10 / 划转 16 / 区划调整 18），并暴露差分体系两大盲区——纯更名不改码、政府驻地迁移
- **补录官方发布页链接 183 条**：`source_official` + `source_url` 写回事件库（民政部年度变更页），其中 2010-2017 占 108 条；`change_events.csv` 同步新增 `source` / `source_official` / `source_url` 三列，对外 CSV 与 JSON 字段一致
- 事件类型新增「驻地迁移」「区划调整」（原差分体系盲区）

### 引擎

- v0.5：修复 4 个真实解析 bug——①3 字通用名（市郊区/市中区等）误当裸别名键跨城误伤 ②同名改码跨省（区划码回收再分配）误生成别名 ③县名短形式被市名劫持（邵阳市东区→邵阳县）④上级不约束县级导致的跨省张冠李戴（邵阳市东区→攀枝花东区）
- 历史别名映射 **521 → 888 条**；黄金测试集回归 104/104 → **110/110**（新增 6 例防回归用例）

## [0.2.0] - 2026-09-12

### 数据（村级全量，GitHub Release 附件）

- **村级实体 604,626 条**：行政村 484,499 + 社区 120,127，覆盖 2,840 县（民政地名口径，日更新源）
- 字段：20 位地名标准码、上级乡镇 9 位码、类别；`villages_v1.json` / `villages.csv.gz` 两种格式
- 全量质检 0 错误；同县同名（不同乡镇）8,721 处为正常现象，以乡镇码区分
- 缺失 7 县为源头零村级记录（台湾省诸县等，官方地名库无数据，不编造）
- 1 条垫江县记录的官方源码本身为 19 位异常，已置空不编造——源头口径问题，如实标注

### 引擎

- v0.3b：多县命中位置优先消歧、市级直配路径特殊口径检查、镇名嵌入文本解析、
  单字别名边界守卫（"京"不劫持"南京/京山市"）、城市别称词典扩容

## [0.1.0] - 2026-09-12

首个公开发布版本。

### 数据
- 省地县三级区划：33 省 / 341 地级 / 2847 县级，官方 12 位码（民政口径，年度版数据截止 2025-12-31，含年内日更新）
- 乡镇街道全量：2,847 县级 / 38,749 条，含区县归属
- **变更事件库 v0.1：5,195 条（1980-2026）**——1980-2020 由 GB/T 2260 官方口径逐年快照差分，2021-2025 手工策展（国函〔2021〕37 号等带文号条目）
- 别名词典 v0.2：历史名/古称/地标/简称/歧义/特殊口径，每条带来源

### 引擎
- engine.py：四级解析、别名与历史名映射（事件库自动生成 489 条）、同名歧义候选集、
  特殊口径诚实标注、拼音错字纠错（可选 pypinyin）、编码直查与历史码回溯、不留存架构

### 已知边界
- 统计口径村级城乡分类代码：无现行公开渠道，不提供、不编造
- 1984 年前事件存在代码重编期噪声
- `doc_no` 字段为增量回填中
